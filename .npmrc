# Skip versions published less than 7 days ago when npm resolves dependencies (npm 11.10.0 or later),
# so that a malicious release is likely found and unpublished before it can be installed here.
min-release-age=7
